
class User {
    constructor(walletAdr, sessionKey, payDate, publicKey, userState) {
      this.walletAdr = walletAdr;
      this.sessionKey = sessionKey;
      this.payDate = payDate;
      this.publicKey = publicKey;
      this.userState = userState;
    }
  
  }
  